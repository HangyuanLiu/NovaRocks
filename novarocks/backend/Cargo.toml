[package]
name = "novarocks-backend"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
novarocks = { path = "../core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "time"] }
