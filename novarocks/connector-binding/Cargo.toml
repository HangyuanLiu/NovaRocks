[package]
name = "novarocks-connector-binding"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
futures = "0.3"
novarocks-proto-codec = { path = "../proto-codec" }
novarocks-spi = { path = "../spi" }

