[package]
name = "novarocks-compat"
version = "0.1.0"
edition = "2024"
autotests = false
build = "build.rs"

[lib]
doctest = false

[dependencies]
novarocks = { path = "../core", features = ["compat"] }
