interface:
  display_name: "Plan Implementation"
  short_description: "Persist a parallel-ready implementation plan"
  default_prompt: "Use $dev-workflow-plan to turn this accepted spec into a persisted dependency-aware task graph for parallel sub-agent execution."
