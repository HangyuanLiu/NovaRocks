---
title: "SC-NNN：<一句话描述这个现场形态>"
date: YYYY-MM-DD
updated: YYYY-MM-DD
type: ops-scenario
id: SC-NNN
status: verified
components: []
symptoms:
  - "<用户口吻：什么现象说明你正处在这个场景里>"
  - "<机器特征：可 grep 的计数 / 日志 / 指标原文>"
versions: []
repos: []
scale:
  <metric>: <实测数值>
repro_fidelity: scaled          # full | scaled | partial
assets: []
jira:
related: []
tags: []
---

# SC-NNN：<标题>

## 1. 真实来源

<哪个客户 / 哪次事故 / 哪个环境，什么时间。没有客户来源就写「构造场景」并说明构造动机。>

## 2. 规模与拓扑

<实测数字，标明测量方式——去重前还是去重后、是否含隐藏对象、怎么数出来的。
估算值必须标「估算」并给出估算方法。>

## 3. 关键环境参数

<那些「默认值咬人」的配置项及其当时取值。每项写清：默认值是多少、当时是多少、为什么它重要。>

| 参数 | 默认值 | 现场取值 | 为什么重要 |
|---|---|---|---|

## 4. 复现方案

> 本节是这条条目存在的理由，不得敷衍。

**缩比策略**：<按什么比例缩、为什么这个比例仍能保住要复现的特征、哪些特征缩比后会失真。>

**步骤**：

1. <...>

**资产入口**：<脚本 / SQL / 配置的路径。既有工具用原路径引用，不要复制。>

**预期耗时**：<...>

## 5. 判定信号

<怎么确认复现成功——可观测的指标 / 日志 / 计数特征。写成可核对的条件，不是「看起来差不多」。>

## 6. 已在此场景暴露的问题

<链到对应的 CS 条目；每条一句话说明它在这个场景下如何被触发。>
